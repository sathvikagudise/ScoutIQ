"""Phase 5 qualification layer.

Qualification turns *persisted, source-backed evidence* into criterion-level
decisions. It is deliberately separated from discovery (Phase 1), research
(Phase 3), extraction/evidence (Phase 4), and lead creation (Phase 5C):

    EXTRACTION != QUALIFICATION

A decision is only ever *derived* from explicit evidence that already exists.
Nothing is fabricated, guessed, inferred from absence, or combined.
"""
